<div class="container-fluid">
<div class="login-wrapper">
    <div class="row">
        <div class="col-md-12">
            <h1>LOGIN</h1>
            <form action="/users/login" method="post">
          

<div class="col-sm-4 col-sm-offset-4">
	<div class="form-group">
    	<input type="text" value="" placeholder="Username" class="form-control" name="username" />
	</div>
    <div class="form-group">
    	<input type="password" value="" placeholder="Password" class="form-control" name="password" />
	</div>
<div class="form-group">
    <button class="btn btn-primary btn-round" type="submit">Submit</button>
</div>
<div class="form-group">
    <a href="/users/register">not yet registered?Click to Register </a>
</div>
</div>

            </form>
        </div>
    </div>
</div>



</div>
    
       <div class="row">
        <div class="col-lg-12">
          {{#if success_msg}}
            <div class="alert alert-success">{{success_msg}}</div>
          {{/if}}

          {{#if error_msg}}
            <div class="alert alert-danger">{{error_msg}}</div>
          {{/if}}

          {{#if error}}
            <div class="alert alert-danger">{{error}}</div>
          {{/if}}
          </div>
    </div> 
    
    
     {{!--  <ul class="nav nav-pills pull-right" id="log">
            {{#if user}}
              <li role="presentation"><a href="/">Dashboard</a></li>
              <li role="presentation"><a href="/users/logout">Logout</a></li>
            {{else}}
              <li role="presentation" class="login"><a href="#"><i class="fa fa-sign-in lel" aria-hidden="true"></i>Login</a></li>
              <br>
              <li role="presentation"><a href="/users/register"><i class="fa fa-user-plus reg-adjust animated pulse infinite" aria-hidden="true"></i>Register</a></li>
            {{/if}}
          </ul> --}}
    
   
 <script src="/js/jquery-3.1.1.js"></script>
  <script>

    </script>
    </div>
    </div>
